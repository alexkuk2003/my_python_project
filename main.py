# ###################################################################################################
# Задание 1.
# функция get_sum принимает два целых числа и возвращает их сумму
def get_sum(a, b):return a + b
print(get_sum(1, 2)) # => 3
print(get_sum(2, 2)) # => 4
# ###################################################################################################
# Задание 2.
# функция count_capital_letters считает количество заглавных букв в строке, синтаксис:

# def count_capital_letters(input_string):
# count = 0
# for char in input_string:
# if char.isupper(): # Проверяем, является ли символ заглавной буквой
# count += 1
# return count

# Переходим к заданию:

# определяем функцию count_capital_letters, которая принимает строку input_string.
# создаем переменную count, которая будет хранить количество заглавных букв.
# с помощью цикла for перебираем каждый символ в строке.
# используем метод isupper(), чтобы проверить, является ли символ заглавной буквой, и, если это так, увеличиваем счетчик на 1.
# в конце функция возвращает общее количество заглавных букв.

print(count_capital_letters("Hello World")) # Вывод: 2
print(count_capital_letters(" A ")) # Вывод: 1

# ###################################################################################################
# Задание 3.
# Создадим функцию decode_string, которая будет обрабатывать входящую строку и возвращать преобразованную строку в соответствии с заданными условиями. Вот сиснтаксис

# def decode_string(s):
# Приводим строку к нижнему регистру
# s_lower = s.lower()

# Создаем словарь для подсчета частоты каждого символа
# frequency = {}
# for char in s_lower:
    frequency[char] = frequency.get(char, 0) + 1

# Формируем новую строку на основе частоты символов
# result = ''
# for char in s_lower:
 #    if frequency[char] == 1:
  #       result += '('  # Один раз встречается
  #   else:
    #     result += ')'  # Более одного раза

# return result

# Переходим к выполнению задания 3:

print(decode_string("din")) # => "((("
print(decode_string("recede")) # => "()()()"
print(decode_string("Success")) # => ")())())"
print(decode_string("(( @")) # => "))(("

# приведение к нижнему регистру: Мы используем s.lower() для того, чтобы игнорировать регистр символов.
# подсчет частоты: Создаем словарь frequency, где ключами будут символы, а значениями — количество их вхождений в строке.
# формирование результата: Проходим по исходной строке и добавляем в результирующую строку (, если символ встречается один раз, и ), если он встречается более одного раза.

 # ################################################################################################### 

  Задание 4.

# создадим функцию get_odd_count, которая будет проходить по каждому символу строки, проверять, является ли он четным числом и подсчитывать количество таких чисел. В соответствии с заданием, ноль считается нечетным.

# синтаксис:

# def get_odd_count(s):
# Инициализируем счетчик четных чисел
# even_count = 0

# Проходим по каждому символу в строке
# for char in s:
    # Преобразуем символ в целое число
   #  num = int(char)
    # Проверяем, является ли число четным
    # if num % 2 == 0 and num != 0:
       #  even_count += 1
        
# return even_count
# Переходим к выполнению задания 4:

print(get_odd_count("2468")) # Вывод: 4
print(get_odd_count("13579")) # Вывод: 0
print(get_odd_count("01234567")) # Вывод: 3

# Инициализация счетчика: мы создаем переменную even_count, которая будет хранить количество четных чисел.
# Цикл по строке: мы проходим по каждому символу строки s.
# Преобразование символа: Каждый символ преобразуется в целое число с помощью int(char).
# Проверка четности: мы проверяем, является ли число четным (num % 2 == 0) и не равно ли оно нулю (num != 0).
# Увеличение счетчика: если оба условия выполняются, увеличиваем счетчик even_count.
# Возврат результата: в конце возвращаем значение счетчика.
  
  # ###################################################################################################
Задание 5.

Синтаксис:
def check_access(has_keycard, has_fingerprint, is_alarm, is_daylight):
# Проверка на сработку сигнализации
if is_alarm:
return False

# Проверка на наличие ключ-карты
if has_keycard:
    # Если есть ключ-карта, требуется дневной свет
    return is_daylight

# Проверка на наличие отпечатка пальца
if has_fingerprint:
    return True

# Если ни ключа, ни отпечатка нет
return False
Переходим к выполнению задания 5:

print("Тест 1 (Ключ, день, нет тревоги):", check_access(True, False, False, True)) # Ожидается: True
print("Тест 2 (Ключ, ночь, нет тревоги):", check_access(True, False, False, False)) # Ожидается: False (для ключа нужен день)
print("Тест 3 (Палец, ночь, нет тревоги):", check_access(False, True, False, False)) # Ожидается: True (для пальца день не нужен)
print("Тест 4 (Палец, день, но ЕСТЬ тревога):", check_access(False, True, True, True)) # Ожидается: False (тревога блокирует все)
print("Тест 5 (Ключ И палец, день, нет тревоги):", check_access(True, True, False, True)) # Ожидается: True
print("Тест 6 (Ничего нет):", check_access(False, False, False, True)) # Ожидается: False

# cначала проверяется, сработала ли сигнализация (is_alarm). Если да, доступ закрыт (возвращаем False).
# если есть ключ-карта (has_keycard), проверяем, есть ли дневной свет (is_daylight). Если дневной свет есть, доступ открыт (возвращаем True), в противном случае — закрыт (возвращаем False).
# если нет ключ-карты, проверяем наличие отпечатка пальца (has_fingerprint). Если отпечаток есть, доступ открыт (возвращаем True).
# если ни ключа, ни отпечатка нет, доступ закрыт (возвращаем False).

# выполнил Алекс К. aka b0bby j0nes
